# rapid-fat-loss-workbook
Rapid Fat Loss Framework Interactive Workbook - modular monolith (React + Express + PostgreSQL). Auth, per-user ownership, deterministic workbook engine, editing/approval/export.
